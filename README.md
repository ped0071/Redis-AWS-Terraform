# Redis-AWS-Terraform

```
terraform init
terraform apply -var-file=prod.tfvars Para utilizar as variáveis de produção
terraform apply -var-file=homolog.tfvars Para utilizar as variáveis de Homolog
```